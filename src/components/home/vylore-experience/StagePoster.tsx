/** Loading placeholder shown while the R3F canvas chunk loads — plain ivory, no mismatched imagery. */
export function StagePoster() {
  return <div className="absolute inset-0 z-10 bg-ivory" aria-hidden="true" />;
}
